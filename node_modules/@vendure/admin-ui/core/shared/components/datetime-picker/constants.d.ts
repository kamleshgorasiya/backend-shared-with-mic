import { DayOfWeek } from './types';
export declare const dayOfWeekIndex: {
    [day in DayOfWeek]: number;
};
export declare const weekDayNames: ("datetime.weekday-su" | "datetime.weekday-mo" | "datetime.weekday-tu" | "datetime.weekday-we" | "datetime.weekday-th" | "datetime.weekday-fr" | "datetime.weekday-sa")[];
